<h1>Productos</h1>

{{#if error}}
  <p style="color:red">{{error}}</p>
{{/if}}

{{#if products.length}}
  <ul>
    {{#each products}}
      <li>{{this.title}} - ${{this.price}}</li>
    {{/each}}
  </ul>
{{else}}
  <p>No hay productos disponibles</p>
{{/if}}

